import 'package:flutter/material.dart';

class AdminUsersPageg extends StatelessWidget {
  const AdminUsersPageg({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Admin Users'),
      ),
    );
  }
}
